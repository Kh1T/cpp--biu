/* 1.Constructors are special member functions that are automatically 
called when an object is created. They are used to initialize objects.

2.Destructors are special member functions that are automatically 
called when an object is destroyed. They are used to release resources.*/

#include <iostream>
using namespace std;

class Car {
    public:
        string brand;

        // Constructor
};